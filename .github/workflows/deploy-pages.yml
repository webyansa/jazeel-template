name: Deploy Jazeel Template to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "github-pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare deployment folder
        run: |
          mkdir -p _site/jazeel _site/donors
          # Copy root index.html
          cp index.html _site/index.html
          # Copy all template files
          cp -r public/jazeel/* _site/jazeel/
          # Copy shared assets
          cp public/jazeel-logo.svg _site/jazeel-logo.svg 2>/dev/null || true
          cp public/placeholder.svg _site/placeholder.svg 2>/dev/null || true
          # Copy donors logos
          cp -r public/donors/* _site/donors/ 2>/dev/null || true
          # Fix redirect path in root index.html
          sed -i 's|./public/jazeel/home.html|./jazeel/home.html|g' _site/index.html

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
